@{
  Layout = "_Layout";
}
@using Factory.Models;

<h1>Dr. Sillystringz's Factory</h1>

<h2>Index of engineers and machines:</h2>

<hr />

<h4>Machines</h4>
@if (Model["machines"].Length == 0)
{
  <p>No machines have been added to the index yet.</p>
} 
<ul>
  @foreach (Machine machine in Model["machines"])
  {
    <li>@Html.ActionLink(@machine.Name, "Details", "Machines", new { id = @machine.MachineId})</li>
  }
</ul>

<h4>Engineers</h4>
@if (Model["engineers"].Length == 0)
{
  <p>No engineers have been added to the index yet.</p>
} 
<ul>
  @foreach (Engineer engineer in Model["engineers"])
  {
    <li>@Html.ActionLink(@engineer.Name, "Details", "Engineers", new { id = @engineer.EngineerId})</li>
  }
</ul>

<hr />

<p>@Html.ActionLink("Manage machines", "Index", "Machines")</p> 
<p>@Html.ActionLink("Manage engineers", "Index", "Engineers")</p>